<h1>Welcome to <%= @user.username %>'s page</h1>
<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>
<h4><%= @user.username %>'s Quizzes </h4>
<div>
<%= will_paginate @user_quizzes %>
</div>
  <%= render 'quizzes/quiz', obj: @user_quizzes %>
  <div>
<%= will_paginate @user_quizzes %>
</div>